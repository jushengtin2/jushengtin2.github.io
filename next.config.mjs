/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/jushengtin.github.io",
    output: "next export",  // <=== enables static exports
    reactStrictMode: true,
  };
  
  module.exports = nextConfig;